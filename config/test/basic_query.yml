log:
  level: INFO

queries:
  - NAME: basic
    OR:
      - machine learning
      - deep learning
    AND:
      - neural network
    NOT:
      - survey

search:
  max_results: 5

output:
  base_dir: output
  formats: [json]

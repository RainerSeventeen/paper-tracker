# Testing

## Config and run

- Run tests: `python -m unittest discover -s test -p "test_*.py"`.
- Test configs: `config/test/`.
- Tests: `test/`.

## Create Test

- There is no need to deploy unit test now. Only write the end-to-end test if needed.
